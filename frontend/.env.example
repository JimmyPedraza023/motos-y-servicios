# URL base de la API FastAPI.
# En desarrollo se puede omitir (usa el proxy de Vite: /api -> http://localhost:8000).
# En producción apuntar al backend desplegado, SIN barra final:
VITE_API_BASE_URL=https://motos-y-servicios.onrender.com